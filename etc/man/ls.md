# Photon@ls
## Advanced file listing tool
### Usage:

`$ ls [switches] [path]`
`$ ls [switches] <pathA> <pathB> <paths...>`

### Examples:
- `$ ls -l / ~`
- `$ ls /home`
- `$ ls -la`
- `$ ls -l -a`

### Switches:

- `-l` : Toggle long list
- `-a` : Show hidden (and protected) files and folders